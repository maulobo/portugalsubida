import styled from "styled-components";

export const ContainerIII = styled.div`
  display: flex;
  flex-direction: column;
  background-color: var(--main-body-color);
  gap: 20px;
  flex-wrap: wrap;
`;
